diff --git a/P3-SAM/demo/auto_mask.py b/P3-SAM/demo/auto_mask.py
--- a/P3-SAM/demo/auto_mask.py
+++ b/P3-SAM/demo/auto_mask.py
@@ -776,8 +776,6 @@ def mesh_sam(
     if show_info:
         print(f"点数：{mesh.vertices.shape[0]} 面片数：{mesh.faces.shape[0]}")
 
-    point_num = 100000
-    prompt_num = 400
     with Timer("获取邻接面片"):
         face_adjacency = mesh.face_adjacency
     with Timer("处理邻接面片"):
@@ -827,7 +825,7 @@ def mesh_sam(
 
     with Timer("推理"):
         bs = prompt_bs
-        step_num = prompt_num // bs + 1
+        step_num = (prompt_num + bs - 1) // bs
         mask_res = []
         iou_res = []
         for i in tqdm(range(step_num), disable=not show_info):
diff --git a/P3-SAM/model.py b/P3-SAM/model.py
--- a/P3-SAM/model.py
+++ b/P3-SAM/model.py
@@ -4,7 +4,6 @@ import torch
 import torch.nn as nn 
 sys.path.append(os.path.join(os.path.dirname(__file__), '..', 'XPart/partgen'))
 from models import sonata
-from utils.misc import smart_load_model
 
 '''
 This is the P3-SAM model.
@@ -15,7 +14,7 @@ The model is composed of three parts:
 '''
 def build_P3SAM(self): #build p3sam
     ######################## Sonata ########################
-    self.sonata = sonata.load("sonata", repo_id="facebook/sonata", download_root='/root/sonata')
+    self.sonata = sonata.load("/models/sonata/sonata.pth")
     self.mlp = nn.Sequential(
             nn.Linear(1232, 512),
             nn.GELU(),
